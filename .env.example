# Default: mock. Set to "jev" to use the real server-side TypeSafe adapter.
VITE_POLICY_MODE=mock
VITE_SEMANTIC_POLICY_URL=/api/semantic-transition

# Read only by vite.config.ts on the server. Never expose this as VITE_*.
TYPESAFE_API_KEY=
